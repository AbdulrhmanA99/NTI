/*
 * WDT_config.h
 *
 * Created: 7/5/2023 11:51:25 PM
 * Author: Abdul
 */ 


#ifndef WDT_CONFIG_H_
#define WDT_CONFIG_H_


// Reset MCU after  16.3    ms	    (Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  32.5    ms		(Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  65      ms      (Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  0.13    s		(Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  0.26    s		(Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  0.52    s		(Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  2.1     s		(Pre-scaler = 6, maps to timeout = 1 second)
// Reset MCU after  1       s		(Pre-scaler = 6, maps to timeout = 1 second)


#endif /* WDT_CONFIG_H_ */